# Stepper · 步骤条

画板：https://www.figma.com/design/NiXwro23h7CvvWT7GOb1ep/?node-id=340-5996

排版对齐「选择器」：顶栏 **Header**（64 标题 + 分类说明）→ 分区 Section（宽 2984、间距 80）：

**1. 申请步骤 → 2. 时间线 → 3. 进度 → 4. 流程总览 → 5. 引导与须知 → 6. 参考原稿**

组件按选择器同一套 **分布规则** 做 Auto Layout：

- 等分项用 **FILL**（选项组）；拉开用 **SPACE_BETWEEN** 或 **间距 FILL**（步骤条）
- 条要 **自适应（全家）**：点/圆直径固定；项 **HUG** 标题；须知项 **FILL**；条是 `[项][间距 FILL + 连线或三角][项]`。拉宽实例，四步跟着铺开，不要写死列宽/间距把内容钉在左边
- 重叠装饰（光晕、底渐变）用 **Absolute** + `STRETCH` / `SCALE`；申请步骤连线放进 **FILL 间距**，不要在条根上叠 Absolute 矢量
- 目录卡：组件集横向、`状态` 格宽 = 变体宽、gap/左 pad 与组件集一致

变体属性统一 **`状态=`**。

规范页上的组件集是「全家福」。要换状态，请点 **实例**（菱形），不要点外面的框。

| 组件 | 右栏能切什么 | 不会跟着变 |
|---|---|---|
| 申请步骤项 AppStepItem | `状态` 已完成 / 当前 / 未到 | `标题`、`序号` |
| 申请步骤条 AppStepper | `状态` 步骤1 / 步骤2 / 步骤3；内部三步已暴露 | 三步标题、序号 |
| 时间线项 TimelineItem | `状态` 已完成 / 进行中 / 待完成；内部点/标已暴露 | `标题` |
| 时间线 TimelineStepper | `状态` 进行中 / 加急中；内部四步已暴露 | 四步标题 |
| 进度点 ProgressDot | `状态` 当前 / 预计 | `序号`、`行1`、`行2`、`副文案` |
| 进度条 ProgressStepper | 目录只展示步骤1当前；实例可切步骤2–4当前；内部四点已暴露 | 四步标题与时长 |
| 流程项 ProcessStepItem | （无状态轴） | `序号`、`标题` |
| 引导项 GuideStepItem | （无状态轴） | `序号`、`副标`、`主标` |
| 须知项 NoticeItem | （无状态轴） | `序号`、`标题`、`说明` |

点条上的一步应选中**项实例**（菱形），不是外框。切状态只换皮，标题和序号还在。

## 变体矩阵

| 组件 | 状态 | 溯源 |
|---|---|---|
| 步骤条/申请步骤点 AppStepDot | 已完成 · 当前 · 未到 | `367:6050` / `367:6043` / `367:6047` |
| 步骤条/申请步骤项 AppStepItem | 已完成 · 当前 · 未到 | 点 + 标题；标题/序号独立 |
| 步骤条/申请步骤条 AppStepper | 步骤1 · 步骤2 · 步骤3 | `367:5998`（无气泡布局）· `367:6179`（有气泡） |
| 步骤条/时间线点 TimelineDot | 已完成 · 进行中 · 待完成 | `367:6408` |
| 步骤条/时间线标 TimelineBadge | 进行中 · 加急中 · 待完成 | `367:6418` · `367:6456` · `367:6420` |
| 步骤条/时间线项 TimelineItem | 已完成 · 进行中 · 待完成 | `367:6408` |
| 步骤条/时间线 TimelineStepper | 进行中 · 加急中 | `367:6408` · `367:6634` |
| 步骤条/进度点 ProgressDot | 当前 · 预计 | `367:6372` |
| 步骤条/进度条 ProgressStepper | 步骤1当前 · 步骤2当前 · 步骤3当前 · 步骤4当前 | `367:6372` |
| 步骤条/流程项 ProcessStepItem | （TEXT 序号/标题） | `367:6633` |
| 步骤条/流程条 ProcessStepper | 默认 | `367:6633` |
| 步骤条/引导项 GuideStepItem | （TEXT 序号/副标/主标） | `367:6487` |
| 步骤条/引导条 GuideStepper | 默认 | `367:6487` |
| 步骤条/须知项 NoticeItem | （TEXT 序号/标题/说明） | `367:6337` |
| 步骤条/须知列表 NoticeList | 默认 | `367:6337` |

未收入：`浮层-发放详情`（`367:6592`）——页面动作说明，非步骤条。

「加急」只体现在 **时间线标 / 时间线**；时间线点本身无单独加急样式（原稿点色仍为蓝系）。

## 1. 申请步骤（蓝底导航）

### 1.1 申请步骤点 AppStepDot

| 状态 | 含义 | 规格 |
|---|---|---|
| 已完成 | 已填完 | 外光晕 85（`#4492FF` + 描边 `#1677FF` 5）· 内圆 56 `#FAFCFF` · 蓝勾 |
| 当前 | 正在填 | 同上光晕结构 · 内圆数字 Inter 30 `#1677FF` |
| 未到 | 未开始 | 圆 60 居中在 **85** 槽 · `#6CA0FF` + 描边 `#1677FF` 4 · 数字蓝 |

### 1.15 申请步骤项 AppStepItem

一列 = 圆点 + 标题。点条上的「填写就医情况」应是这个实例（菱形），不是「点槽」框。

- `状态`：已完成 / 当前 / 未到（只换圆点）
- `标题`：填写就医情况等，切换状态不改
- `序号`：1 / 2 / 3，切换状态不改（当前切到未到仍显示 2）

申请步骤条由三个 AppStepItem 实例组成。

### 1.2 申请步骤条 AppStepper

宽 **750**，底 `#1677FF`。三步始终都在：填写基本信息 → 填写就医情况 → 一键上传材料。切右栏 **`状态`**，不要从实例里删某一步。

Auto Layout：`[AppStepItem HUG][间距 FILL + 连线][AppStepItem][间距 FILL + 连线][AppStepItem]`，左右 pad **44**。未到圆 60 居中在 85 槽。连线是间距里的 4px 矩形（不是单独的线组件），`STRETCH` 随间距变长，向左接到上一颗圆。

右栏只有一轴 **`状态`**：步骤1 / 步骤2 / 步骤3。

| 状态 | 三项 | 连线 | 点击查看/修改 | 布局 |
|---|---|---|---|---|
| 步骤1 | 当前 / 未到 / 未到 | 两段都 `#6CA0FF` | 无 | padTop **21**（无气泡，对齐 `367:5998` 的点高） |
| 步骤2 | 已完成 / 当前 / 未到 | 1→2 白，2→3 `#6CA0FF` | 有，第 1 步右侧 `(160, 24)` | padTop **42**。溯源 `367:6179` |
| 步骤3 | 已完成 / 已完成 / 当前 | 两段都白 | 有，同位置 | padTop **42** |

步骤1 没有「点击查看/修改」；到步骤2 才出现，步骤3 也保留。气泡 Absolute，不撑开列宽。

标题：已完成 / 当前 PingFang SC Semibold 24 白；未到 Regular 24 白。

## 2. 时间线

### 2.1 时间线点 TimelineDot

圆 **48**。

| 状态 | 规格 |
|---|---|
| 已完成 | 底 `#DAF7FF` + 蓝勾 |
| 进行中 | 底 `#9FDFFF` + 内描边 `#DAF7FF` 12 |
| 待完成 | 空心描边 `#DAF7FF` 4 |

### 2.2 时间线标 TimelineBadge

高 **40**、圆角 **12**、左右 pad **12**。

| 状态 | 底 | 字 |
|---|---|---|
| 进行中 | `#1677FF` @5% | Medium 24 `#1677FF` |
| 加急中 | `#F93A4A` @5% | Medium 24 `#F93A4A` |
| 待完成 | `#F5F5F5` | Regular 24 `#999` @70% |

### 2.3 时间线项 TimelineItem

一列 = 点 + 标题 + 可选标。点条上的「保险公司审核」应是这个实例，不是套着的 Frame。

- `状态`：已完成 / 进行中 / 待完成（只换点/标；已完成隐藏标）
- `标题`：提交申请等，切换状态不改
- 内部暴露：`点`（TimelineDot）、`标`（TimelineBadge）

### 2.4 时间线 TimelineStepper

卡宽 **638**、圆角 **32**。四步是 TimelineItem 实例：提交申请 → 材料初审 → 保险公司审核 → 待打款。间距 **FILL**，连线画在间距里（`#d8f6ff` 2px）。拉宽实例，列距跟着走。底渐变 Absolute `STRETCH`。

| 状态 | 差异 |
|---|---|
| 进行中 | 第 3 步标「进行中」 |
| 加急中 | 第 3 步标「加急中」（点样式不变） |

## 3. 进度（含时长）

### 3.1 进度点 ProgressDot

序号条 **93×40** 底 `#E7F1FF`，号 Alibaba Sans Bold 26 `#1677FF`；标题两行 Semibold 26 `#333`；副文案 Regular 24 `#666`。

TEXT：`序号`、`行1`、`行2`、`副文案`。切 `当前` / `预计` 不改标题。

| 状态 | 副文案示例 |
|---|---|
| 当前 | 当前 |
| 预计 | 约0-1日 / 约1-2日 / 约1-3日 |

### 3.2 进度条 ProgressStepper

宽 **702**。四步是 ProgressDot 实例：提交申请 → 材料初审 → 理赔审核 → 赔付打款。

列距 **FILL**（默认 702 宽时约 79）。虚线画在间距里，不要写死 spacer 把四步钉在左边。拉宽实例，四列跟着铺开。

**目录只放「步骤1当前」一条**（对照 `367:6372`）。步骤2–4当前变体藏在集里，实例右栏仍可切。

## 4. 流程总览

### 4.1 流程项 ProcessStepItem

一列 = 序号 Alibaba Bold 28 + 标题 PingFang Medium 26，宽 **104**。TEXT：`序号`、`标题`。原稿四步同色，无状态轴。

### 4.2 流程条 ProcessStepper

奶油底 `#FFFAED`、描边 `#EEE`、圆角 **16**、**670×142**。四步是 ProcessStepItem 实例：材料初审 → 理赔调查 → 理赔审核 → 理赔完结。间距 **FILL**，连线在间距内。拉宽跟着走。

原稿无进度色差，仅 **状态=默认**。

## 5. 引导与须知

### 5.1 引导项 GuideStepItem

一列 = 64 圆 `#E9FAFF` + 序号 Alibaba Bold 32 `#1677FF` + 副标 Regular 24 `#999` + 主标 Semibold 24 `#333`。圆与副标间距 **10**，副标与主标 **2**（总高 **156**）。

TEXT：`序号`、`副标`、`主标`。原稿四步同皮肤，无状态轴。

### 5.2 引导条 GuideStepper

宽 **638**、高 **218**。四步是 GuideStepItem 实例：电子医保就医 → 帮选保单 → 协助理赔 → 通知理赔进度。间距 **FILL**，原稿三角放在间距里居中，不是 `›`。底渐变 Absolute `STRETCH`。仅 **状态=默认**。

### 5.3 须知项 NoticeItem

一行 = 序号 Alibaba Regular 32 + 标题 Medium 28 + 说明 Regular 28 `#555` @70%。宽 **606**。说明可多行（子弹用换行，行高 46、段距 4）。左侧竖线在序号轨内，与说明对齐。

TEXT：`序号`、`标题`、`说明`。

### 5.4 须知列表 NoticeList

四条是 NoticeItem 实例，纵向间距 **20**。宽 **FILL**（默认 606），说明文本跟着撑。仅 **状态=默认**。

## 状态选用说明

- **需要 `状态` 变体的**：申请步骤点/项/条、时间线点/标/项/条、进度点、进度条（当前步位置）。
- **单态即可的**：流程项/条、引导项/条、须知项/列表（原稿无可切换视觉态；文案走 TEXT）。
- **加急**：只改时间线标文案/色，不要另做红点原子（无产品 UI 证据）。
- **点得着**：条里的每一步必须是项实例。套 Frame 的「编组 / 项」右栏没有变体。
